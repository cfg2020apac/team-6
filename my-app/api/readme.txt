Ensure the following flask libraries are installed by running:

pip install flask

pip install Flask-SQLAlchemy

pip install mysql-connector-python

pip install flask_cors

To start, run the flask application with the following command:
	python "filename".py

Install the program "Postman" to simulate API calls against the backend